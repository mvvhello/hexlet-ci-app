name: workflow1
on: push
jobs: 
    build: 
        runs-on: ubuntu-latest
        steps: 
            - name: Checkout code
              uses: actions/checkout@v3
              
            - name: set up environment
              run: make setup

            - name: Run tests
              run: make test

            - name: Lint the code
              run: make lint